using Microsoft.AspNetCore.Mvc;

namespace GooseAPI.Controllers
{


    [ApiController]
    
    public class SleepDataController : Controller
    {
        //[HttpGet("/api/sleep")]
        //public IActionResult Get([FromQuery] string apiKey, [FromQuery] string date)
        //{
        //    string userName = GooseAPI.GooseAPIUtils.FindUserNameByAPIKey(apiKey);
        //    User userData = GooseAPI.GooseAPIUtils.GetUser(userName);
        //    if(userName == null) {
        //        return BadRequest(new {message = "there is no user for this "})
            
            
        //    }
        //}
    }
}
